#
# tuned configuration
#

[cpu]
governor="<%= node['dynamic-perf']['balance_govenor'] %>"
energy_perf_bias="<%= node['dynamic-perf']['balance_energy_perf_bias'] %>"

[audio]
timeout="<%= node['dynamic-perf']['balance_audio] %>"

[video]
radeon_powersave="<%= node['dynamic-perf']['balance_radeon_powersave'] %>"

[disk]
# Comma separated list of devices, all devices if commented out.
# devices=sda .each do
