#
# tuned configuration
#

[cpu]
governor="<%= node['dynamic-perf']['powersave_govenor'] %>"
energy_perf_bias="<%= node['dynamic-perf']['powersave_energy_perf_bias'] %>"

[eeepc_she]

[vm]

[audio]
timeout="<%= node['dynamic-perf']['powersave_timeout'] %>"

[video]
radeon_powersave="<%= node['dynamic-perf']['powersave_radeon_powersave'] %>"

[disk]
# Comma separated list of devices, all devices if commented out.
# devices=sda
alpm="<%= node['dynamic-perf']['powersave_alpm'] %>"

[net]
# Comma separated list of devices, all devices if commented out.
# devices="<%= node['dynamic-perf']['powersave_net'] %>"
# . each do ????????

[sysctl]
vm.laptop_mode="<%= node['dynamic-perf']['powersave_vm_laptop_mode'] %>"
vm.dirty_writeback_centisecs="<%= node['dynamic-perf']['powersave_vm_dirty_writeback_centisecs'] %>"
kernel.nmi_watchdog="<%= node['dynamic-perf']['powersave_kernel_nmi_watchdog'] %>"

[script]
script="<%= node['dynamic-perf']['powersave_script'] %>"
